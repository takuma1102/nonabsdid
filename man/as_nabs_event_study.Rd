% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_nabs_event_study.R, R/tidy_dcdh.R,
%   R/tidy_panelmatch.R, R/tidy_fect.R, R/naive_twfe.R, R/tidy_list.R
\name{as_nabs_event_study}
\alias{as_nabs_event_study}
\alias{as_nabs_event_study.default}
\alias{as_nabs_event_study.nabs_event_study_tbl}
\alias{as_nabs_event_study.data.frame}
\alias{as_nabs_event_study.did_multiplegt_dyn}
\alias{as_nabs_event_study.PanelEstimate}
\alias{as_nabs_event_study.fect}
\alias{as_nabs_event_study.fixest}
\alias{as_nabs_event_study.list}
\title{Coerce an estimator result to a tidy event-study tibble}
\usage{
as_nabs_event_study(x, method = NULL, outcome = NA_character_,
               conf.level = 0.95, ...)

\method{as_nabs_event_study}{PanelEstimate}(x, method = NULL,
  outcome = NA_character_, conf.level = 0.95,
  pre_obj = NULL, add_reference = TRUE, ...)
}
\arguments{
\item{x}{A supported estimator object.}

\item{method}{Optional override for the \code{method} column.}

\item{outcome}{Optional outcome name to record in the \code{outcome} column.}

\item{conf.level}{Confidence level for \code{conf.low} / \code{conf.high}.
Default \code{0.95}.}

\item{...}{Method-specific arguments.}

\item{pre_obj}{(\code{PanelEstimate} method only.) A \code{placebo_test} result
from \code{PanelMatch::placebo_test()}.}

\item{add_reference}{(\code{PanelEstimate} method only.) Logical; add a
\code{(time = -1, estimate = 0)} row.}
}
\value{
A tibble of class \code{"nabs_event_study_tbl"}.
}
\description{
\code{as_nabs_event_study()} is an S3 generic that converts the native output object
of a supported estimator into the unified \emph{nabs_event_study_tbl} schema.
}
\details{
\subsection{PanelMatch method}{

For \code{PanelMatch::PanelEstimate()} the
post-treatment leads are stored as \code{$estimate} / \code{$standard.error} (singular).
The pre-treatment placebo results from
\code{PanelMatch::placebo_test()} use
\code{$estimates} / \code{$standard.errors} (plural). To produce a single
event-study path, pass the placebo object via \code{pre_obj}.
}

\subsection{fect method}{

\code{fect::fect()} returns event-study coordinates in
\code{$time} and \code{$att}, with confidence-interval bounds in \code{$att.bound}.
Standard errors are pulled from \code{$est.att[, "S.E."]} when available.
}

\subsection{fixest method}{

Extracts coefficients on \code{time_to_event} interactions
(built by \code{\link[=naive_twfe]{naive_twfe()}} with \code{fixest::i()}).
}
}
\examples{
\dontrun{
fit <- DIDmultiplegtDYN::did_multiplegt_dyn(
  df = mydata, outcome = "y", group = "id", time = "t",
  treatment = "d", effects = 6, placebo = 4
)
tidy_fit <- as_nabs_event_study(fit, outcome = "y")
}
}
